#k8s 
A machine that runs containers within the cluster
- Run actual workloads, containers inside of pods
- Runs and manages containers on the node
- Monitors the state of containers on the node and reports the status back to the [[K8S Control Plane]]

#flashcards/k8s
What runs on a Worker Node?::Actual workloads, containers inside of pods

Worker Node reports container status back to the ==Control Plane==

Worker Node:::A machine that runs containers within the cluster, manages them, and reports status to the Control Plane