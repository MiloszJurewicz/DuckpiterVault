A **Deployment Controller** is responsible for **lifecycle management of stateless applications**—creating, updating, scaling, monitoring, and rolling back the [[K8S ReplicaSet]] that run your Pods.

#flashcards/k8s
What is the Deployment Controller responsible for?::Lifecycle management of stateless applications—creating, updating, scaling, monitoring, and rolling back ReplicaSets

Deployment Controller manages the lifecycle of ==stateless applications==
<!--SR:!2026-06-23,0,230-->